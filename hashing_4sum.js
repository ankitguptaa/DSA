/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[][]}
 */
var fourSum = function(nums, target) {
    for (let i = 0; i < nums.length; i++) {
        for (let j = j; j < nums.length; j++) {
            
        }
    }
};

let nums = [1,0,-1,0,-2,2], target = 0;
console.log(fourSum(nums, target));
nums = [2,2,2,2,2];
target = 8;
console.log(fourSum(nums, target));